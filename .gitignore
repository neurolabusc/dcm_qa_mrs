# Untracked output directories — regenerated by the comparison scripts.
Out/
spec2nii/

# Defensive: if a user clones spec2nii_test_data inside this repo (instead of
# alongside it), keep it out of git. Cloning outside the tree is recommended
# (see README), but this guard prevents a 5 GB accidental stage.
spec2nii_test_data/

# Python / macOS cruft
__pycache__/
*.pyc
.DS_Store
